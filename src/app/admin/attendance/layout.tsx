export const metadata = {
  title: 'Attendance Management - PackTrack',
}

export default function AttendanceAdminLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
